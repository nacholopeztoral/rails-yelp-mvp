<h1>My list of restaurants</h1>
<% @restaurants.each do |restaurant| %>
<h2><%= link_to restaurant.name, restaurant_path(restaurant)%><%= link_to 'Add review', new_restaurant_review_path(restaurant.id),
class: "btn btn-primary"%></h2>

<% end %>
<%= link_to 'New Restaurant', new_restaurant_path,  class: "btn btn-primary"  %>
